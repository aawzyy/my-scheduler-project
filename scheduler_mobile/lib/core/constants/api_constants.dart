class ApiConstants {
  // Ganti dengan URL AWS / Cloudflare Anda yang sudah dideploy
  // Untuk Emulator Android gunakan 10.0.2.2, untuk Real Device gunakan IP LAN atau Domain Asli
  static const String baseUrl = "https://aawzyy.my.id";

  // Endpoints
  static const String appointments = "$baseUrl/api/appointments";
  static const String auth = "$baseUrl/api/auth";
}
